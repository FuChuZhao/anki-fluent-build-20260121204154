# Anki Fluent Desktop — Public CI Workflows

This repository intentionally contains **only** GitHub Actions workflows.

- Source code lives in a private repo (cloned via read-only deploy key).
- Workflows are **manual only** (workflow_dispatch).
